<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="de.thm.ap.groupexpenses">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".view.activity.EventActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.EventFormActivity" />
        <activity android:name=".view.activity.PositionActivity" />
        <activity android:name=".view.activity.PositionFormActivity" />
        <!--<activity android:name=".view.PositionInspectActivity" />-->
        <!--<activity android:name=".view.AddUsersActivity" />-->
        <activity
            android:name=".view.activity.FriendsActivity"
            android:exported="true"
            android:label="@string/title_activity_friends"/>
        <activity
            android:name=".view.activity.BountyActivity"
            android:exported="true" />
        <activity
            android:name=".view.activity.LoginActivity"
            android:exported="true"
            android:theme="@style/ThemeOverlay.MyNoTitleActivity" />
        <activity android:name=".view.activity.ProfileActivity" />
        <activity android:name=".view.activity.TabTestActivity"
            android:exported="true"/>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="@string/file_provider_authority"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>