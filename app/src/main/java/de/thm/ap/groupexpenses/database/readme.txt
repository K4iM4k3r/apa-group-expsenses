How to use Firebase:

1. Install Node.js
2. Install Firebase CLI (console: npm install -g firebase-tools)
3. Add the project (console: firebase add)

Relevant functions are written in file index.js (functions folder).
Any changes or additions have to be deployed to the server (console: firebase --deploy only functions). 


